# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

class_name: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
#
# Enabling the following modules runs them for every test in every class!
#
# For specific tests that need L5, consider using LaravelTestCase instead.
#
#        - Laravel5:
#            environment_file: .env.testing
#        - Doctrine2:
#            connection_callback: ['DoctrineSetup', 'createEntityManager']
#            cleanup: true
